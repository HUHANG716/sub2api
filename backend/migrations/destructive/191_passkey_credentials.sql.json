{
  "migration": "191_passkey_credentials.sql",
  "phase": "review",
  "reason": "Creates passkey credential and stable user-handle tables required for WebAuthn authentication.",
  "risk": "The migration creates two non-concurrent indexes and foreign-key-backed tables, which can briefly acquire catalog and table locks while the schema objects are created.",
  "mitigation": "Both indexes target newly created empty tables, and existing users are not backfilled, so index creation does not scan or block writes to existing production rows.",
  "approved_by": "hang"
}
